- alias: Color Temp Fix - Living Room - Night
  hide_entity: True
  trigger:
    - platform: state
      entity_id: group.living_room_lights
      to: 'on'
    - platform: state
      entity_id: sensor.color_temp_lr
      state: '278'
  condition:
    condition: or
    conditions:
      - condition: time
        after: '22:30:00'
      - condition: time
        before: '05:00:00'
  action:
    service: light.turn_on
    entity_id: group.living_room_lights
    data:
      color_temp: 500
      transition: 30
