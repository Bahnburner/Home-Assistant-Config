movie_mode_on:
  sequence:
    - service: light.turn_on
      entity_id: light.movie_mode
      data_template:
        brightness: 
          '{{states.light.living_room.attributes.brightness | int}}'
    - service: light.turn_off
      entity_id:
        - light.table_lamp
        - light.cabinet_light
        - light.recessed_lights
        - light.overhead_light
        - light.chandelier
    - service: light.turn_on
      entity_id:
        - light.floor_lamp_left
        - light.floor_lamp_right
      data:
        brightness: 60
        color_temp: 500
        transition: 15

movie_mode_off:
  sequence:
    - service: light.turn_on
      entity_id: light.living_room
      data:
        transition: 15
      data_template:
        brightness:
          '{{states.light.movie_mode.attributes.brightness | int }}'
        color_temp: >
          {% if (states.sensor.time.state | round(2)) <= 05.00 %}
            500
          {% elif (states.sensor.time.state | round(2)) <= 09.50 %}
            250
          {% elif (states.sensor.time.state | round(2)) < 22.25 %}
            366
          {% elif (states.sensor.time.state | round(2)) >= 22.25 %}
            500
          {% else %}
            366
          {% endif %}
